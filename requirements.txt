# Installs everything needed to run both halves of the app from one place.
# See backend/requirements.txt and frontend/requirements.txt for the
# per-service lists this is kept in sync with.

# --- backend (FastAPI) ---
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
langchain>=1.0
langchain-openai>=1.0
python-dotenv>=1.0

# --- frontend (Streamlit) ---
streamlit>=1.38
requests>=2.28
watchdog>=4.0
